create or replace TABLE CONFIG.PIPELINE (
	PIPELINEID VARCHAR(36) NOT NULL DEFAULT UUID_STRING(),
	PIPELINENAME VARCHAR(255) NOT NULL,
	SOURCEID VARCHAR(36) NOT NULL,
	DESTINATIONID VARCHAR(36) NOT NULL,
	LOADTYPEID NUMBER(38,0),
	LAYERID NUMBER(38,0),
	CUSTOMQUERY VARCHAR(16777216),
	UNIQUEKEYCOLUMNS VARCHAR(2000),
	EXPECTEDLOADTIME NUMBER(38,0),
	CRONID VARCHAR(500),
	ISARCHIVEAFTERDATARETENTION BOOLEAN,
	DATARETENTIONARCHIVEPATH VARCHAR(1000),
	DATARETENTIONTYPE VARCHAR(50),
	DATARETENTIONPERIOD NUMBER(38,0),
	FRAMEWORKTYPE VARCHAR(50),
	STAGE VARCHAR(1000),
	WAREHOUSE VARCHAR(1000),
	NOTEBOOKID VARCHAR(36),
	ISDEPENDENCY VARCHAR(4000),
	"ORDER" NUMBER(10,0),
	RETRYDELAYSECONDS NUMBER(38,0) DEFAULT 60,
	WATERMARKCOLUMN VARCHAR(255),
	ACTIVE BOOLEAN,
	CREATEDDATETIME TIMESTAMP_NTZ(9),
	MODIFIEDDATETIME TIMESTAMP_NTZ(9),
	ENGINE VARCHAR(16777216),
	ISDELETE BOOLEAN,
	ALERTCONFIG VARCHAR(16777216),
	constraint UK_PIPLINE_NEW_PIPELINENAME unique (PIPELINENAME)
)COMMENT='DataAccel is driven by configuration tables that controls the entire data lifecycle. These tables defines all ingestion, transformation, validation, scheduling, and governance logic in one place.\n\nBy using one unified configuration layer, DataAccel eliminates the need for multiple metadata tables, scripts, and custom workflows—making data processing simpler, faster, and easier to manage and govern at scale.For Reference: Pipeline Table tEST'
;
